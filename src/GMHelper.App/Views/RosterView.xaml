<UserControl x:Class="GMHelper.App.Views.RosterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:GMHelper.App"
             xmlns:views="clr-namespace:GMHelper.App.Views"
             mc:Ignorable="d">
    <Grid Margin="{StaticResource SubViewMargin}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="{StaticResource GutterMargin}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Button Grid.Row="0" Content="Neuer Charakter" HorizontalAlignment="Left" Margin="0,0,0,12" Command="{Binding NewCharacterCommand}" />

            <TextBlock Grid.Row="1"
                       Text="{Binding StatusMessage}"
                       Style="{StaticResource StatusTextStyle}"
                       Visibility="{Binding StatusMessage, Converter={x:Static local:NullToVisibilityConverter.Instance}}" />

            <ListBox x:Name="PlayerList"
                     Grid.Row="2"
                     ItemsSource="{Binding Players}"
                     SelectedItem="{Binding SelectedPlayer}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <CheckBox IsChecked="{Binding IsActive}" Click="ActiveCheckBox_Click" VerticalAlignment="Center" Margin="0,0,8,0" />
                            <TextBlock Text="{Binding CharacterName}" VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                <Setter Property="Foreground" Value="Gray" />
                                                <Setter Property="FontStyle" Value="Italic" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>

        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="Charaktername" Style="{StaticResource FieldLabelTextStyle}" />
                <TextBox Text="{Binding EditCharacterName, UpdateSourceTrigger=PropertyChanged}" Margin="{StaticResource FieldMargin}" />

                <TextBlock Text="Spieler (Person)" Style="{StaticResource FieldLabelTextStyle}" />
                <TextBox Text="{Binding EditPlayerName, UpdateSourceTrigger=PropertyChanged}" Margin="{StaticResource FieldMargin}" />

                <TextBlock Text="Initiative" Style="{StaticResource FieldLabelTextStyle}" />
                <TextBox Text="{Binding EditInitiativeText, UpdateSourceTrigger=PropertyChanged}" Width="80" HorizontalAlignment="Left" Margin="{StaticResource FieldMargin}" />

                <TextBlock Text="Notizen" Style="{StaticResource FieldLabelTextStyle}" />
                <TextBox Text="{Binding EditNotes, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" Height="72" TextWrapping="Wrap" Margin="{StaticResource FieldMargin}" />

                <TextBlock Text="Werte" Style="{StaticResource SectionHeaderTextStyle}" />
                <views:StatFieldEditor ItemsSource="{Binding StatFields}" Height="220" Margin="{StaticResource FieldMargin}" />

                <StackPanel Orientation="Horizontal">
                    <Button Content="Speichern" Command="{Binding SaveSelectedPlayerCommand}" />
                    <Button Content="Charakter löschen" Command="{Binding DeleteSelectedPlayerCommand}" Margin="8,0,0,0" />
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
