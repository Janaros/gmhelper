<UserControl x:Class="GMHelper.App.Views.RosterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:GMHelper.App"
             xmlns:views="clr-namespace:GMHelper.App.Views"
             mc:Ignorable="d">
    <Grid Margin="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="0,0,8,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Button Grid.Row="0" Content="Neuer Charakter" HorizontalAlignment="Left" Margin="0,0,0,8" Command="{Binding NewCharacterCommand}" />

            <TextBlock Grid.Row="1"
                       Text="{Binding StatusMessage}"
                       Foreground="Red"
                       TextWrapping="Wrap"
                       Margin="0,0,0,8"
                       Visibility="{Binding StatusMessage, Converter={x:Static local:NullToVisibilityConverter.Instance}}" />

            <ListBox x:Name="PlayerList"
                     Grid.Row="2"
                     ItemsSource="{Binding Players}"
                     SelectedItem="{Binding SelectedPlayer}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <CheckBox IsChecked="{Binding IsActive}" Click="ActiveCheckBox_Click" VerticalAlignment="Center" Margin="0,0,4,0" />
                            <TextBlock Text="{Binding CharacterName}" VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                <Setter Property="Foreground" Value="Gray" />
                                                <Setter Property="FontStyle" Value="Italic" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>

        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="Charaktername" />
                <TextBox Text="{Binding EditCharacterName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

                <TextBlock Text="Spieler (Person)" />
                <TextBox Text="{Binding EditPlayerName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

                <TextBlock Text="Initiative" />
                <TextBox Text="{Binding EditInitiativeText, UpdateSourceTrigger=PropertyChanged}" Width="80" HorizontalAlignment="Left" Margin="0,0,0,8" />

                <TextBlock Text="Notizen" />
                <TextBox Text="{Binding EditNotes, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" Height="60" TextWrapping="Wrap" Margin="0,0,0,8" />

                <TextBlock Text="Werte" FontWeight="Bold" Margin="0,0,0,4" />
                <views:StatFieldEditor ItemsSource="{Binding StatFields}" Height="200" Margin="0,0,0,8" />

                <Button Content="Speichern" HorizontalAlignment="Left" Command="{Binding SaveSelectedPlayerCommand}" Padding="8,4" Margin="0,0,0,8" />
                <Button Content="Charakter löschen" HorizontalAlignment="Left" Command="{Binding DeleteSelectedPlayerCommand}" Padding="8,4" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
