<UserControl x:Class="GMHelper.App.Views.CampaignDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:GMHelper.App.Views"
             xmlns:tools="clr-namespace:Syncfusion.Windows.Tools.Controls;assembly=Syncfusion.Tools.WPF"
             mc:Ignorable="d">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,12">
            <Button Content="&lt; Zurück" Command="{Binding BackCommand}" />
            <TextBlock Text="{Binding Campaign.Name}" FontSize="18" FontWeight="Bold" Margin="12,0,0,0" VerticalAlignment="Center" />
        </StackPanel>

        <tools:TabControlExt Grid.Row="1">
            <tools:TabItemExt Header="PDFs">
                <views:PdfLibraryView DataContext="{Binding PdfLibrary}" />
            </tools:TabItemExt>
            <tools:TabItemExt Header="Bilder">
                <views:ImageLibraryView DataContext="{Binding ImageLibrary}" />
            </tools:TabItemExt>
            <tools:TabItemExt Header="Spieler">
                <views:RosterView DataContext="{Binding Roster}" />
            </tools:TabItemExt>
            <tools:TabItemExt Header="Kampf">
                <views:CombatTrackerView DataContext="{Binding CombatTracker}" />
            </tools:TabItemExt>
            <tools:TabItemExt Header="Notizen">
                <views:SessionNotesView DataContext="{Binding SessionNotes}" />
            </tools:TabItemExt>
        </tools:TabControlExt>
    </Grid>
</UserControl>
