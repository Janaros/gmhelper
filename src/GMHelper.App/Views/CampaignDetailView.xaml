<UserControl x:Class="GMHelper.App.Views.CampaignDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:GMHelper.App.Views"
             mc:Ignorable="d">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,12">
            <Button Content="&lt; Zurück" Command="{Binding BackCommand}" />
            <TextBlock Text="{Binding Campaign.Name}" FontSize="18" FontWeight="Bold" Margin="12,0,0,0" VerticalAlignment="Center" />
        </StackPanel>

        <TabControl Grid.Row="1">
            <TabItem Header="PDFs">
                <views:PdfLibraryView DataContext="{Binding PdfLibrary}" />
            </TabItem>
            <TabItem Header="Bilder">
                <views:ImageLibraryView DataContext="{Binding ImageLibrary}" />
            </TabItem>
            <TabItem Header="Spieler">
                <TextBlock Text="Kommt in Phase 5 (Roster mit flexiblen Stats)." Margin="12" Foreground="Gray" />
            </TabItem>
            <TabItem Header="Kampf">
                <TextBlock Text="Kommt in Phase 8 (Kampf-Tracker)." Margin="12" Foreground="Gray" />
            </TabItem>
            <TabItem Header="Notizen">
                <TextBlock Text="Kommt in Phase 9 (Markdown-Session-Notizen)." Margin="12" Foreground="Gray" />
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
