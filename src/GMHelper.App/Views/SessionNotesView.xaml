<UserControl x:Class="GMHelper.App.Views.SessionNotesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:GMHelper.App"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             mc:Ignorable="d">
    <Grid Margin="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="0,0,8,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Button Grid.Row="0" Content="Neue Notiz" HorizontalAlignment="Left" Margin="0,0,0,8" Command="{Binding NewNoteCommand}" />

            <TextBlock Grid.Row="1"
                       Text="{Binding StatusMessage}"
                       Foreground="Red"
                       TextWrapping="Wrap"
                       Margin="0,0,0,8"
                       Visibility="{Binding StatusMessage, Converter={x:Static local:NullToVisibilityConverter.Instance}}" />

            <ListBox Grid.Row="2"
                     ItemsSource="{Binding Notes}"
                     SelectedItem="{Binding SelectedNote}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Text="{Binding Title}" FontWeight="Bold" />
                            <TextBlock Text="{Binding SessionDate, StringFormat='{}{0:dd.MM.yyyy}'}" Foreground="Gray" FontSize="11" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                <TextBox Width="300" Text="{Binding EditTitle, UpdateSourceTrigger=PropertyChanged}" />
                <DatePicker Width="130" Margin="8,0,0,0" SelectedDate="{Binding EditSessionDate}" />
            </StackPanel>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBox x:Name="MarkdownTextBox"
                         Grid.Column="0"
                         Text="{Binding EditMarkdownContent, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True"
                         AcceptsTab="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         FontFamily="Consolas"
                         Margin="0,0,4,0" />

                <Border Grid.Column="1" BorderBrush="LightGray" BorderThickness="1" Margin="4,0,0,0">
                    <wv2:WebView2 x:Name="PreviewWebView" />
                </Border>
            </Grid>

            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,8,0,0">
                <Button Content="Speichern" Command="{Binding SaveSelectedNoteCommand}" Padding="8,4" />
                <Button Content="Notiz löschen" Margin="8,0,0,0" Command="{Binding DeleteSelectedNoteCommand}" Padding="8,4" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
