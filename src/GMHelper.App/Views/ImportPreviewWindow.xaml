<Window x:Class="GMHelper.App.Views.ImportPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Monster-Import – Vorschau"
        Icon="/Assets/GMHelper.ico"
        Width="640" Height="480"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="{StaticResource ViewMargin}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DataGrid Grid.Row="0"
                  ItemsSource="{Binding Records}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  Margin="0,0,0,12">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="160" />
                <DataGridTextColumn Header="Notizen" Binding="{Binding Notes}" Width="*" />
                <DataGridTextColumn Header="Bild" Binding="{Binding ImagePath}" Width="140" />
                <DataGridTextColumn Header="Werte" Binding="{Binding Stats.Count}" Width="60" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBlock Text="Bei vorhandenem Namen:" VerticalAlignment="Center" Margin="0,0,8,0" />
            <ComboBox ItemsSource="{Binding ConflictStrategies}" SelectedItem="{Binding ConflictStrategy}" Width="140" Margin="0,0,16,0" />
            <Button Content="Importieren" Click="ImportButton_Click" Margin="0,0,8,0" />
            <Button Content="Abbrechen" Click="CancelButton_Click" />
        </StackPanel>
    </Grid>
</Window>
