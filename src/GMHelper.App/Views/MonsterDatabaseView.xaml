<UserControl x:Class="GMHelper.App.Views.MonsterDatabaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:GMHelper.App"
             xmlns:views="clr-namespace:GMHelper.App.Views"
             mc:Ignorable="d">
    <Grid Margin="{StaticResource ViewMargin}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,16">
            <Button Content="&lt; Zurück" Command="{Binding BackCommand}" />
            <TextBlock Text="Monster-Datenbank" Style="{StaticResource PageTitleTextStyle}" Margin="12,0,0,0" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="240" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Margin="{StaticResource GutterMargin}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                    <Button Content="Neues Monster" Command="{Binding NewMonsterCommand}" />
                    <Button Content="Importieren" Margin="8,0,0,0" Click="ImportButton_Click" />
                    <Button Content="Exportieren" Margin="8,0,0,0" Click="ExportButton_Click" />
                </StackPanel>

                <TextBlock Grid.Row="1"
                           Text="{Binding StatusMessage}"
                           Style="{StaticResource StatusTextStyle}"
                           Visibility="{Binding StatusMessage, Converter={x:Static local:NullToVisibilityConverter.Instance}}" />

                <ListBox x:Name="MonsterList"
                         Grid.Row="2"
                         ItemsSource="{Binding Monsters}"
                         SelectedItem="{Binding SelectedMonster}"
                         DisplayMemberPath="Name" />
            </Grid>

            <Grid Grid.Column="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="220" />
                </Grid.ColumnDefinitions>

                <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" Margin="{StaticResource GutterMargin}">
                    <StackPanel>
                        <TextBlock Text="Name" Style="{StaticResource FieldLabelTextStyle}" />
                        <TextBox Text="{Binding EditName, UpdateSourceTrigger=PropertyChanged}" Margin="{StaticResource FieldMargin}" />

                        <TextBlock Text="Notizen" Style="{StaticResource FieldLabelTextStyle}" />
                        <TextBox Text="{Binding EditNotes, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" Height="72" TextWrapping="Wrap" Margin="{StaticResource FieldMargin}" />

                        <TextBlock Text="Werte" Style="{StaticResource SectionHeaderTextStyle}" />
                        <views:StatFieldEditor ItemsSource="{Binding StatFields}" Height="220" Margin="{StaticResource FieldMargin}" />

                        <StackPanel Orientation="Horizontal">
                            <Button Content="Speichern" Command="{Binding SaveSelectedMonsterCommand}" />
                            <Button Content="Monster löschen" Command="{Binding DeleteSelectedMonsterCommand}" Margin="8,0,0,0" />
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>

                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Button Grid.Row="0" Content="Bild zuweisen" Margin="0,0,0,12" Click="AssignImageButton_Click" />
                    <Image x:Name="PortraitImage" Grid.Row="1" Stretch="Uniform" />
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
